rootProject.name = 'quezap'

dependencyResolutionManagement {
    repositories {
        mavenCentral()
    }
    versionCatalogs {
    }
}

include(
    ":modules:domain",
    ":modules:shared",
    ":modules:application",
    ":modules:infrastructure",
    ":quezap-api",
    ":quezap-cli"
)
